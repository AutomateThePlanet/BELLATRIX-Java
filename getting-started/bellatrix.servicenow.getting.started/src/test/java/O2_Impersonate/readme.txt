Impersonate Tests :

1. For the tests at least two users are expected to be set on the instance
    - the first user's userName and password will be read from the config file
    - the second user's name is a test parameter
      Replace "User Impersonate" in the test with the actual user.
